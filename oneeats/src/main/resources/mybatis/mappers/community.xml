<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
	
<mapper namespace="com.example.demo.community.dao.CommunityDAO">
	
	<resultMap id="recipeResult" type="recipeVO">
	    <result property="recipeNo" column="recipeNo" />
	    <result column="title" property="title"/>
		<result column="cooking_time" property="cooking_time"/>
		<result column="description" property="description"/>
		<result column="cookingImg" property="cookingImg"/>
		<result column="category" property="category"/>
		<result column="inbun" property="inbun"/>
		<result column="creDate" property="creDate" javaType="java.sql.Date"/>
		<result column="memberNo" property="memberNo"/>
	</resultMap>
	
	<resultMap id="ingredientResult" type="ingredientVO">
		<result property="recipeNo" column="recipeNo" />
		<result property="name" column="name" />
		<result property="qty" column="qty" />
	</resultMap>
	
	<select id="sellectAllRecipe" resultMap="recipeResult" >
	    <![CDATA[
			sellect * from recipe
		]]>
	</select>
	
	
	<select id="selectRecipeList" resultMap="recipeResult" parameterType="java.util.Map" >
	    <![CDATA[
			select * from recipe
			order by creDate desc
			limit 6
		]]>
	</select>
	
	<select id="selectNewRecipeList" resultMap="recipeResult">
		select * from recipe order by creDate desc limit 3
	</select>
	
	<select id="selectRecipeByRecipeNo" resultType="recipeVO" >
		select * from recipe where recipeNo=#{recipeNo}
	</select>
	
	
	<select id="selectIngredientByRecipeNo" resultMap="ingredientResult"  parameterType="int">
		select * from ingredient where recipeNo=#{recipeNo}
	</select>
	
	
	<select id="selectNewRecipeNo" resultType="int" >
		select ifnull(max(recipeNo),0)+1 from recipe
	</select>

	<insert id="insertNewRecipe" parameterType="recipeVO">
		insert recipe (recipeNo,title,cooking_time,description,cookingImg,category,inbun,memberNo) values(#{recipeNo},#{title},#{cooking_time},#{description},#{cookingImg},#{category},#{inbun},#{memberNo})			
	</insert>
	
	<insert id="insertNewRecipeWithMap" parameterType="java.util.Map">
		insert recipe (recipeNo,title,cooking_time,description,cookingImg,category,inbun,memberNo) values(#{recipeNo},#{title},#{cooking_time},#{description},#{cookingImg},#{category},#{inbun},#{memberNo})
	</insert>
	
	<insert id="insertIngredients" parameterType="java.util.List" >
		insert into ingredient (recipeNo,name,qty)
		values
		<foreach collection="list" item="i" index="index" separator=",">
			(#{i.recipeNo}, #{i.name}, #{i.qty})
		</foreach>
	</insert>
	
	<insert id="insertIngredientsWithMap" parameterType="java.util.Map" >
		insert into ingredient (recipeNo,name,qty)
		values
		<foreach collection="#{ingredientList}" item="i" index="index" separator=",">
			(#{i.recipeNo}, #{i.name}, #{i.qty})
		</foreach>
	</insert>
</mapper>